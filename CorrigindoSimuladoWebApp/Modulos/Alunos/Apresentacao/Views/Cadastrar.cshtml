@model CadastrarAlunoViewModel
@{
    Layout = "_Layout";
    ViewBag.Titulo = "Cadastro de Aluno";
}

<div class="row justify-content-center my-3 my-md-4">
    <div class="col-12 col-md-10 col-lg-8 col-xl-7 col-xxl-6">

        <form asp-controller="Aluno" asp-action="Cadastrar" method="post">

            <!-- Card Principal -->
            <div class="p-4 p-md-5 bg-white shadow-sm rounded-4 border-start border-5"
                 style="border-color: #0f2d59 !important;">

                <!-- Cabeçalho -->
                <div class="mb-4">
                    <h3 class="fw-bold mb-2" style="color: #0f2d59; letter-spacing: -0.5px;">
                        <i class="bi bi-person-plus me-2"></i>Cadastrar Novo Aluno
                    </h3>
                    <p class="text-muted fs-6 mb-0">Preencha os dados abaixo para vincular um novo aluno ao sistema.</p>
                </div>

                <!-- Grid de Campos -->
                <div class="row g-3 g-md-4">

                    <!-- Nome Completo (Linha inteira) -->
                    <div class="col-12">
                        <label asp-for="Nome" class="form-label fw-semibold" style="color: #0f2d59;"></label>
                        <input asp-for="Nome" class="form-control form-control-lg shadow-sm"
                               placeholder="Digite o nome completo do aluno" />
                        <span asp-validation-for="Nome" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <!-- Nº de Matrícula (40% da largura em telas médias+) -->
                    <div class="col-12 col-md-5">
                        <label asp-for="NumeroDeMatricula" class="form-label fw-semibold" style="color: #0f2d59;">Nº de
                            Matrícula</label>
                            <input asp-for="NumeroDeMatricula" type="text" inputmode="numeric" maxlength="7"
                                   class="form-control form-control-lg shadow-sm" placeholder="Ex: 1234567" />
                            <span asp-validation-for="NumeroDeMatricula" class="text-danger small mt-1 d-block"></span>
                        </div>

                        <!-- Seleção de Turma (60% da largura em telas médias+) -->
                        <div class="col-12 col-md-7">
                            <label asp-for="TurmaId" class="form-label fw-semibold" style="color: #0f2d59;">Turma</label>
                            <select asp-for="TurmaId" class="form-select form-select-lg shadow-sm">
                                <option value="">Selecione uma turma...</option>
                                @foreach (SelecionarTurmaViewModel turma in Model.TurmasDisponiveis!)
                                {
                                    <option value="@turma.Id">@turma.Nome</option>
                                }
                            </select>
                            <span asp-validation-for="TurmaId" class="text-danger small mt-1 d-block"></span>
                        </div>

                    </div>

                    <!-- Divisória -->
                    <hr class="my-4 text-muted" style="opacity: 0.15;">

                    <!-- Botões de Ação -->
                    <div class="d-grid gap-2 d-sm-flex justify-content-sm-end">

                        <a asp-controller="Aluno" asp-action="IndexAlunos"
                           class="btn btn-lg bg-white shadow-sm rounded-pill px-4 text-nowrap order-2 order-sm-1"
                           style="color: #0f2d59; border: 2px solid #0f2d59; transition: 0.2s;">
                            <i class="bi bi-arrow-left me-2"></i>Voltar
                        </a>

                        <button type="submit"
                                class="btn btn-lg text-white shadow-sm rounded-pill px-4 text-nowrap order-1 order-sm-2"
                                style="background-color: #0f2d59; border: 2px solid #0f2d59; transition: 0.2s;">
                            <i class="bi bi-check-lg fs-5 me-2"></i>Cadastrar Aluno
                        </button>

                    </div>

                </div>
            </form>

        </div>
    </div>